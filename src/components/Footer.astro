---
import { ChessKnight } from '@lucide/astro'
import { brandingConfig } from '../config/branding.config'
---

<footer class="footer sm:footer-horizontal bg-base-200 text-base-content p-10">
  <aside>
    <ChessKnight />
    <p class="text-lg font-bold">{brandingConfig.site.name}</p>
    <p class="text-base-content/60">{brandingConfig.site.tagline}</p>
  </aside>
  {brandingConfig.footerGroups.map(({ title, links }) => (
    <nav>
      <h6 class="footer-title">{title}</h6>
      {links.map(({ label, href }) => (
        <a href={href} class="link link-hover">{label}</a>
      ))}
    </nav>
  ))}
  <nav>
    <h6 class="footer-title">Connect</h6>
    {brandingConfig.social.map(({ platform, href }) => (
      <a href={href} class="link link-hover">{platform}</a>
    ))}
  </nav>
</footer>
<div class="bg-base-200 w-full">
<hr class="border-base-300 mx-10" />
</div>
<footer class="footer footer-center bg-base-200 text-base-content p-4">
  <p>© {new Date().getFullYear()} {brandingConfig.copyright}. All rights reserved.</p>
</footer>
